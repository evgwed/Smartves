@model Smatrves.Models.ShowReport

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ShowReport</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DateTimeShow)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DateTimeShow)
            @Html.ValidationMessageFor(model => model.DateTimeShow)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IpViewer)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IpViewer)
            @Html.ValidationMessageFor(model => model.IpViewer)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
